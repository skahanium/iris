import Paragraph from "@tiptap/extension-paragraph";

/** Iris-owned paragraph node. Block spacing is Markdown separation, not editable spacer nodes. */
export const IrisParagraphExtension = Paragraph.extend({});
