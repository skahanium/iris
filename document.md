# Iris Agent 施工文档体系定稿方案

## 1. 体系范围与文档组织

采用已确认的方式：**规范长期维护，施工记录完成后归档；全局合同先确定，再沿 D0–D5 按阶段细化。**

文档统一置于 `agent-harness/`，不恢复旧 HR 阶段的权威和完成度。

| 区域              | 职责                                                            |
| ----------------- | --------------------------------------------------------------- |
| 入口与维护规则    | 阅读路径、权威边界、状态、复核、变更及归档规则                  |
| 需求与当前基线    | 需求来源、源码事实、已知缺陷、未决项                            |
| `modules/`        | 9 份模块规范，逐项展开 C01–C27                                  |
| `contracts/`      | 共享接口、状态转换、不变量及异常合同                            |
| `tools/`          | 39 个业务工具的独立卡片，以及协议入口、Planned、外部 MCP 的区别 |
| `flows/`          | 对话与修订、联网、编辑、格式保持、上下文与记忆、委派六条链路    |
| `testing/`        | 正反例、覆盖关系、证据组合与验收要求                            |
| `implementation/` | D0–D5 依赖、工作包和执行记录                                    |
| `decisions/`      | 架构变更、细化、复核和替代记录                                  |
| `registry.json`   | 身份、正式定义位置、版本、类型化关联及状态登记                  |

讨论纪要负责解释来源，架构定义负责上位决定，新体系负责详细规范和施工依据。执行结果继续放入既有 `docs/eval/results/`，不复制证据仓库。

组件规范必须交代输入输出、决定权、状态、不变量、异常恢复、审计、源码落点、兼容和测试。工具卡进一步明确参数消费、授权、副作用、预算、幂等、取消与失败反馈。链路文档说明交接顺序，引用正式合同，不重复定义它们。

本体系不负责版本工期，不自动证明合同合理或工具可用，不替代真实模型验收，也不读取模型私有思维链。

## 2. 登记、正式定义与关系模型

### 登记不等于正式定义

采用 DeepSeek 建议的 **“已登记／已有正式定义”两级结构**：

- **已登记**：具备稳定 ID、种类、责任归属、内容摘要和明确缺口；正式定义位置为空，不能伪装成有效链接。
- **已有正式定义**：具备真实文件、唯一对象范围、修订和指纹；仍可以处于草案。
- **可施工**：正式定义及全部必要依赖均完整，影响复核已经处理，作用域内没有改变实现的未决问题。

第一批登记全部模块、组件和工具，但不创建空白文档桩。登记对象可以作为待补齐关系的目标；任何可施工工作包都不能依赖只有登记、尚无定义的必要对象。

已声明存在正式定义却找不到文件或对象范围，是错误，不能退回“仅登记”绕过检查。

### 正式定义和全文归属

一个对象只有一个正式定义位置。详细规范使用成对标记：

```markdown
<!-- iris:object C20 -->

<a id="C20"></a>

正式定义正文。

<!-- iris:end C20 -->
```

规则固定为：

- 对象标记必须独占一行、成对匹配、不嵌套；代码围栏中的示例不作为标记解析。
- ID 是不可复用身份。重命名身份须创建新 ID 并登记替代关系；移动同一对象不改变 ID，但必须更新位置并进入影响检查。
- 对象指纹包含标记、标题和全部范围内正文，仅统一换行编码，不忽略可能有语义的空白或标点。
- 每份托管文件必须登记文件级对象，承接所有对象块之外的内容。
- 文件级指纹包含外围正文及子对象的有序占位标识，不包含子对象正文；因此可以分别识别前言变化、对象移动和对象内容变化。
- 子对象隐式依赖所在文件的公共说明。公共说明变化会使这些对象进入复核，不能形成无人负责的规范文字。
- 未登记文件、重复范围、标记损坏和内容无法归属均阻断检查。

讨论纪要等尚未采用分块标记的上位材料，以文件级来源对象绑定版本；初期接受较保守的影响范围，不悄悄漏检。

### 类型化关系

关系允许多对多，不采用单一父节点。只有施工依赖和替代关系分别要求无环；运行反馈由链路描述。

边的端点及处理方式明确如下：

| 边           | 固定方向                  | 变化后的处理                                       |
| ------------ | ------------------------- | -------------------------------------------------- |
| `implements` | 实现对象 → 要求／合同     | 要求或合同定义变化时，沿入边查找实现对象，进入复核 |
| `consumes`   | 消费者 → 被消费对象       | 被消费定义变化时，沿入边查找消费者，进入复核       |
| `verifies`   | 测试 → 被验证声明／不变量 | 被验证定义变化时，测试设计和关联证据适用性进入复核 |
| `depends_on` | 依赖者 → 前置对象         | 前置状态变化时，沿入边重新计算依赖者的就绪状态     |
| `blocks`     | 问题 → 工作包／验收声明   | 问题状态变化时，沿出边重新计算指定门槛             |
| `supersedes` | 新对象 → 旧对象           | 从旧对象的消费者关系启动迁移检查；禁止自动改指     |

`blocks` 必须注明阻断“开始”还是“验收”。某缺陷通常阻止验收，不应因此阻止修复它的工作包开始。

问题关闭只移除该问题造成的阻断。其他问题、过期合同、缺失证据或未完成前置仍然有效；不会自动把工作包提升为可施工或已验收。

## 3. 变更传播、复核与解封

### 变更分类

改变责任归属、状态权威、输入输出语义、决定权、不变量、权限、副作用、兼容或用户承诺，属于架构变更，必须同步架构并记录决定。

只有在上述承诺保持不变时，字段落实、内部实现、示例和说明才属于细化。新增字段、调整默认值或超时不能自动被视为细化。

检查器发现变更后要求给出分类和理由，不声称能机械判断语义是否等价。检查器规则、复核权限和验收门槛自身的变化也属于治理变更，按架构变更复核。

### 传播机制

注册表绑定对象修订、正文指纹、依赖版本，以及相关源码、测试和证据身份。

检查器区分两种事件：

- **定义或实现变化**：根据关系类型产生复核和重新验证要求。
- **问题、前置或复核状态变化**：重新计算就绪和阻断，不自动判定定义失效。

传播使用工作队列去重处理，保留每项影响的原因路径。上游影响未解决时，下游不能通过局部确认提前恢复有效状态。

检查器只计算和报告，不静默修改文档或历史记录。登记修订与实际内容不匹配时直接报错，不能只更新一个哈希就抹去影响记录。

### 独立复核

采用用户确认的规则：**架构变更后的“无影响”结论须由独立 AI 或人工复核。**

复核记录必须绑定：

- 变更 ID、对象 ID、变更前后修订及内容指纹。
- 具体差异范围、受影响对象和判断理由。
- 变更作者、复核者及可追溯评审记录。
- 无影响、已同步、需重新验证或被替代的处理结论。

架构变更的作者不能自行作出无影响结论解除阻断。独立 AI 指未参与该变更编写的评审任务；不同署名或模型名称本身不构成独立性证据。

纯细化允许作者自审，但必须引用具体差异和理由。改变行为或验证判据时，无论由谁复核，都必须补充相应验证，不能用“无影响”代替测试。

检查器核验记录完整性、身份区别、关联版本和评审记录是否存在；不把字符串比较宣称为身份认证或真正独立性的证明。人员及 AI 身份与评审来源由维护者管理，不新增复杂的签名平台。

### 历史与当前适用性

继续保留文档成熟度、实现状态、验证结果和工作包状态四个维度。

历史测试通过不可改写为失败；历史验收也不删除。另行计算这些记录对当前版本是有效、待复核还是不再适用。当前“可施工／已验收”的展示必须依据有效性计算，不能只读取历史标签。

替代关系保留旧身份和版本。当前消费者必须明确迁移；历史记录可以继续绑定旧版本，但不能据此宣称新版本已验证。

## 4. 证据与违例检测要求

证据采用用途分类及组合要求，不采用“高级证据替代低级证据”的单一等级：

| 验收对象                         | 必需证据                               |
| -------------------------------- | -------------------------------------- |
| 局部机械不变量                   | 确定性正例和违例检测                   |
| 跨组件交接、续轮、取消、来源绑定 | 实际组件组合或生产入口测试，含异常路径 |
| 厂商端点及协议兼容               | 协议测试＋对应端点真实调用             |
| 相关性、追问承接、文风等语义质量 | 真实模型场景＋明确判据＋人工抽检       |
| 诊断界面与交互                   | 状态投影测试＋界面交互或人工检查       |
| 完整用户任务                     | 该任务涉及的全部必需证据               |

跨层权限、写入等性质必须覆盖最终执行边界，不能仅凭局部校验函数通过。

每个不变量登记合法案例、违例案例、预期检测位置、错误或恢复行为，以及对应测试。负例测试应在正确实现中成功发现违规，不是保留一个永久失败的测试。

具体缺陷优先证明旧实现失败、修复后通过；其他不变量可用故障注入、受控错误响应或变异验证。检查器要求反例关联完整，执行与审阅负责证明测试确实能区分错误实现。

证据须绑定规范、测试、源码、环境及非敏感配置版本。未运行、空分母、不适用和失效证据不能变成必需维度的通过。模块及工作包验收由具体声明汇总，不能拿一个测试覆盖全部能力。

## 5. 分批建立与检查器自身可靠性

### 交付批次

1. **基础设施批**：入口、治理规则、完整对象登记、最小已定义样例、检查器及负例测试。其余对象明确仅登记。
2. **全局规范批**：按模块和链路逐批补齐正式定义，处理现行文档与旧计划的权威冲突。
3. **施工准备批**：D0 先达到可直接执行；D1–D5 按依赖逐批细化。

入口、注册规则、索引及检查器在基础设施批接通，不要求全部规范正文同时落地。有效旧任务逐项迁移；已退役材料按清单归档，不批量继承或抹去历史状态。

### 阻断规则

从第一批起：

- 注册表不可解析、必需文件无法读取、正式定义解析失败、脚本异常或超时，一律阻断。
- 结构检查失败返回非零；执行基础设施失败也返回非零，并区别于“发现规范问题”。
- 不捕获异常后返回空集合，不把无法检查解释为没有问题。
- CI 不允许对文档门使用忽略失败或成功兜底；只有全部检查完整执行且通过，才输出通过。
- 草案缺少内容进入缺口报告；一旦声明可施工或已验收，完整性、复核和证据要求立即成为阻断条件。

### 检查器测试

至少覆盖：

- 多消费者共享一个合同，合法且不复制定义。
- 各边类型的定义变化、状态变化及传播方向。
- 一个问题关闭但另一阻断仍存在，工作包不能解封。
- 只有登记的对象可作为草案存在，但不能支持可施工声明。
- 正式定义缺失、重复锚点、标记损坏、对象移动及外围正文变化。
- C20 变更使有关对象进入复核，无关对象保持有效。
- 架构作者自签无影响不能解封；有效独立复核能按范围解除影响。
- 复核绑定旧差异、缺少理由或新版本继续变化时不能解封。
- 历史通过保留，但不足以支持变化后的当前验收。
- 缺少反例、证据类型不匹配及空分母不能通过验收。
- 注册表损坏、文件读取失败和检查器异常均返回失败，不出现伪通过。

首批成功标准是：**全局对象登记完整，正式定义与草案可以区分，变化能够传播，解封具有依据，检查失败不会放行，D0 能交给另一位工程师直接施工。** 后续阶段保持真实成熟度，不为追求全绿提前声明完成。
