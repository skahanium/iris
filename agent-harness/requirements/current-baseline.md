# 当前基线与未决问题

<!-- iris:object P04 kind=rules file=true -->

本文件记录**当前基线的源码事实与待证事项**。它不声明修复完成，也不把历史故障归因成唯一根因；每一条未决问题都注明已确认的事实、影响边界与所需证据。

**维护规则**：只登记能在注明基线上核对的事实；无法核对的写「未知」并登记缺口；历史观察不得改写成当前承诺；问题关闭只移除该问题造成的阻断，不改变其他条件。

## 一、事实基线

| 范围             | 基线                                                                                            |
| ---------------- | ----------------------------------------------------------------------------------------------- |
| 架构定义基线     | `2670739f`（`docs/agent-architecture.md` 的源码核对基线；行号变化不改变符号定位）               |
| 讨论纪要首轮审查 | `b9198619` 及 2026-09-14 当时工作树；期间存在并行修改，未提交代码中的新问题不倒推为此前故障原因 |
| 讨论纪要两轮复核 | `2d357038`，2026-09-15                                                                          |
| 历史故障         | 2026-09-14 开发版运行记录；记录不足处保留未知原因                                               |
| 本体系建立       | 以 `2670739f` 与 `2d357038` 的静态核对为准，**未执行新实验，未运行付费实网评测**                |

来源对象与指纹见 `registry.json` 的 `sources` 段（`SRC-DISC-2026-09-14`、`SRC-ARCH-2026-09-15`）。来源变化会使引用它的对象进入复核。

## 二、当前目录事实

对基线 `2670739f` 的 `collect_tool_catalog` 七组定义逐项统计（依据 `tool_catalog/groups.rs`、`tool_catalog_impl.rs`）：

| 组       | Dispatchable | HarnessOnly | Planned |
| -------- | -----------: | ----------: | ------: |
| read     |            9 |           2 |       0 |
| vault    |            5 |           0 |       0 |
| web      |            2 |           0 |       0 |
| boundary |           11 |           0 |      11 |
| write    |            2 |           0 |       0 |
| root     |            8 |           0 |       0 |
| skills   |            1 |           0 |       0 |
| 合计     |           38 |           2 |      11 |

共 51 个内置目录项。这里的「可派发」是**源码声明状态**，不代表已逐个实测生产效果；不含动态外部 MCP 与独立注入的结构化提交。

目标目录为 39 个业务工具（`T01`–`T39`），与当前 38 个可派发项的差异见 [工具总览](../tools/README.md) 与各 `T*` 卡片的「当前状态」。

## 三、未决问题（`Q*`）

问题身份使用稳定编号与符号定位，行号变化不改变问题含义。这些问题**不自动构成**此前 MiniMax 对话故障的唯一根因。以下条目是文件级对象之外的子对象，与文件级对象同级。

<!-- iris:end P04 -->

<!-- iris:object Q01 kind=issue owner=M09 -->

### Q01 历史未知工具提议无法事后唯一归因

- **已确认事实**：2026-09-14 的故障描述为初始搜索和抓取成功后连续两轮目录外工具，进入 `recovery_exhausted`（三轮模型、两次工具）。当时 dev 库早于 migration `075`，没有 `audit_boundary_events`，不能对那次 Run 做 `assistant_run_diagnose`。现行 C11 记录声明名与解析名指纹及 parsePath／rewritten，C17 记录 catalog／surface／declared／mapping 与派发结果，C27 按回合与工具面版本关联；缺 hop 不得归因于模型。当前代码下同类轨迹由 `diagnose_run`（与 `assistant_run_diagnose` 同一查询）演练，见 [诊断演练](../../docs/eval/results/2026-09-14-diagnostic-drill.md)。
- **调查结论**：历史那一次的唯一根因**永久无法确定**（记录不足，不是尚未修复的代码缺陷）。同类故障的可定位性由 `D01` 交付。关闭本项表示调查结束，不表示已证实模型或 MCP 为根因。
- **不能推出**：六次网络额度用完；模型或 MCP 是 2026-09-14 那次的唯一根因；现行未知工具提议一定是模型错误。未派发的提议不按执行路径增加网络工具计数，网络额度与「两轮拒绝收束」是两条独立规则。
- **影响边界**：`C11` 流式拼装后的调用身份、`C16` 工具面版本、`C17` 拒绝原因三处都可能产生同类现象。
- **覆盖**：来源链与 `diagnose_run` 端到端演练已接入；历史那一次仍不能事后唯一归因。

<!-- iris:end Q01 -->

<!-- iris:object Q02 kind=issue owner=M06 -->

### Q02 设置写入冲突：两个入口的 MCP 搜索候选语义不一致

- **已确认事实**：`McpProfilesPanel` 读取并保存完整有序候选数组；`useAiSidecarBridge` 只展示首项，管理中心单选变更写入单元素数组。缺陷是两个设置入口的**写入语义不一致**，会丢失已有备用项。
- **不能推出**：「整个前端从来只能配置一条路线」。
- **影响边界**：`C16`、`C18`、`C25`；对应架构定义 `F01`。
- **所需证据**：两个设置入口交替保存的往返测试，以及「原生＋单 MCP」「MCP 主备切换」的生产入口验证（`V04`）。

<!-- iris:end Q02 -->

<!-- iris:object Q03 kind=issue owner=E03 -->

### Q03 评测空分母被记为满分

- **已确认事实**：`verdict.rs` 的 `ratio_bps` 在分母为 0 时返回 `10000`；`aggregate_capacity_scorecard` 据此计算召回、引用与约束门。当前实现先汇总原子分子分母，并非逐用例百分比取平均，`atoms` 完全为空也会报错。
- **不能推出**：「每个无要求用例都会拉高混合评测分数」。缺陷是某个维度总分母为零却仍可标为满分通过。
- **影响边界**：`E01`、`E03`；对应架构定义 `F02`。
- **所需证据**：区分「适用／未覆盖／通过」并显示实际分母的判据；零分母维度不得作为质量证据放行（`V01`）。
- **覆盖**：已由聚合负例覆盖；历史 v1.2.15 报告仍为旧口径。

<!-- iris:end Q03 -->

<!-- iris:object Q04 kind=issue owner=M04 -->

### Q04 流式终止原因丢失（Chat Completions 分支）

- **已确认事实**：此前 Chat Completions 流式分支没有消费 `choices[0].finish_reason`，正常返回及尾缓冲早退写死 `stop`。现行路径由 `ChatCompletionsStreamState` 消费该字段，缺省为 `unknown`（与非流式一致）；`length` 等到 `FinalAnswerIntegrity`，截断工具不派发。
- **不能推出**：所有协议都忽略终止原因——同文件 Anthropic 状态机已有停止原因处理，Responses 走独立路径。Anthropic 五类终止负例绑定不在本条，也不在 `G06`。本条覆盖不关闭 `D02`／`Q10`／`G06`，也不替代逐端点 `V05`。
- **影响边界**：`C11`、`C23`、`C14`；对应架构定义 `F03`。
- **所需证据**：覆盖正常结束、长度截断、工具结束、尾事件与缺失终止事件的协议测试。**本条的关闭证据是 Chat Completions 的机械记录（`V03`）**，见 `registry.json.verify`；逐端点 `V05` 是本条的验证目标但**不是关闭门槛**——它由 `G03`／`Q17`／`D04` 承接（2026-09-17 关闭门槛改写）。
- **覆盖**：Chat Completions 流式已保留供应商终止事实（缺省 `unknown`，不填 `stop`）；截断工具不派发；Anthropic／Responses 仍不在本条范围。Anthropic Messages 流式五类负例已另绑 `C11`／`K04`，不扩大本条关闭条件。`V03` 机械记录见 `registry.json.verify`；不把 `Q04`／`C11` 的 `verification.state` 标为通过。本条由 `D02` 验收关闭，关闭只表示已确认的 Chat Completions 缺陷已修复并有机械覆盖，**不表示任一端点已 live 通过**。

<!-- iris:end Q04 -->

<!-- iris:object Q05 kind=issue owner=M06 -->

### Q05 读取路径写入数据库：普通列举触发隐藏配置变更

- **已确认事实**：`mcp_runtime_registry.rs` 的 `list_web_evidence_providers` 调用 `heal_legacy_search_result_limit_mappings`，为匹配的旧配置补字段并 UPDATE 映射、哈希与时间。`mcp_external_tools.rs` 仍比较绑定哈希与当前 provider 哈希，不一致时拒绝，因此**未被证明绕过所有漂移拒绝**。
- **不能推出**：已导致某次用户故障——仍需关联运行证据。配置内容实际变化后重算哈希本身是必要的，问题在变更时机、可见性与已有绑定的处理。
- **影响边界**：`C18`、`C04`、`C26`；对应架构定义 `F04`。
- **所需证据**：查询保持只读、配置升级移到明确保存边界的验证；既有信任与快照不随新哈希自动扩权。

<!-- iris:end Q05 -->

<!-- iris:object Q06 kind=issue owner=M05 -->

### Q06 子任务参数声明与执行不一致

- **已确认事实**：`spawn_subagent` 的工具定义提供 `context_hint`，执行路径没有消费；`max_rounds` 没有作为实际轮次控制输入，执行采用冻结预算；报告置信度按是否有 evidence ID 固定填 50 或 75。
- **不能推出**：子 agent 整体没有价值或不能做任何对照。
- **影响边界**：`C15`、`C16`、`C17`。
- **所需证据**：参数消费或明确拒绝的合同实现，以及子任务报告的真实来源、未解决问题与用量（`V04`）。

<!-- iris:end Q06 -->

<!-- iris:object Q07 kind=issue owner=M05 -->

### Q07 子工具缺少标准网页正文抓取

- **已确认事实**：子工具清单包含 `web_search`，没有 `web_fetch`，限制标准网页正文核验。
- **不能推出**：「无法读取任何正文」——`read_note` 等本地工具仍可读取材料。
- **影响边界**：`C15`、`C21`。
- **所需证据**：子任务获准使用 `web_fetch` 后的对照结果与总成本、耗时记录。

<!-- iris:end Q07 -->

<!-- iris:object Q08 kind=issue owner=M03 -->

### Q08 会话压缩覆盖边界与裁剪边界不同源

- **已确认事实**：近期历史受 token 和消息对数限制，模型压缩却主要按消息条数确定覆盖终点；少量长消息可能先被裁剪但尚未进入摘要覆盖范围。系统已有覆盖不完整检测，因此不是完全静默丢失。
- **检测位置（2026-09-19 核对）**：`run_context.rs` 的 `history_coverage_is_incomplete(memory, recent_messages)` 比较摘要覆盖终点与**真实的 token 预算后历史视图**首条序号（`seq_end + 1 < first_recent.seq`），装配时把结果写入 `conversation_history_coverage_incomplete`，再由 `conversation_memory_prompt_fragment` 把 `CONVERSATION_HISTORY_COVERAGE_WARNING` 追加进 provider 看到的历史块（`run_context.rs` 常量定义处）。因此合同要求的「一致，**或**留下显式缺口」由它满足：缺口不会被当成已覆盖。
- **不能推出**：发现缺口与补齐缺口是不同能力；也不能预设「收紧联网次数会加速裁剪」。
- **影响边界**：`C08`、`C07`、`K07`、`K08`。
- **所需证据**：被裁剪区间必须有可解释的覆盖或缺口；摘要不得覆盖用户后来的纠正（`V02`）。
- **覆盖**：检测与显式缺口均已在位——`partial_memory_marks_an_omitted_middle_range_for_the_model`（有缺口时报缺口并把「历史覆盖边界」写进模型可见的历史块）、`contiguous_memory_and_recent_history_do_not_claim_a_gap`（连续时不得谎报缺口）、`latest_explicit_correction_is_preserved_as_a_constraint`（后续更正必须保留为约束）、`refresh_keeps_summary_and_recent_window_disjoint_at_twenty_five_messages`（摘要与近期窗口不重叠）。`V03` 机械记录见 `registry.json.verify`；本条由 `D03` 验收关闭。
- **已知限制（本条关闭不等于已消除）**：覆盖终点仍由 `conversation_memory.rs` 的 `refresh_for_session` 按**消息条数**（`recent_message_limit`）计算，而裁剪边界由 `select_bounded_recent_history` 按 **token 预算**计算——两者不同源这一事实**没有改变**。不同源不是合同禁止的状态（合同允许「留下显式缺口」），但它意味着缺口会周期性出现，补缺依赖警告而不是靠边界对齐。把覆盖终点改为与 token 预算同源是一项独立的后续改进，登记在此，不在本条关闭条件内。

<!-- iris:end Q08 -->

<!-- iris:object Q09 kind=issue owner=M09 -->

### Q09 诊断关联不完整：能看到动作，未必能还原因果

- **已确认事实**：工具审计保存运行、步骤、工具、摘要、成功状态与耗时；循环诊断的界面快照仍是部分计数、首次失败及最近 12 条事件。`C26` 把循环恢复事件与出站结构见证追加到独立表，缺握手结束标为未知。`C27` 按会话与显式 `runId` 解释这些事件，并按模块 → 组件 → 调用实例呈现；工具名来源见 `Q01`／`Q12`。
- **不能推出**：已经有日志或已有结构见证就等于语义理解已被证明。
- **影响边界**：`C26`、`C27`、`C11`、`C17`。
- **所需证据**：出站结构证据（消息角色与数量、必要字段是否存在、修复消息是否纳入、所用协议、前后边界关联标识）与 Run／模型轮次／尝试／工具调用／父子任务的关联（`V08`）。
- **覆盖**：C26 记录与 C27 查询／V07 界面投影已覆盖所需关联；界面快照 12 条截断不再是权威。

<!-- iris:end Q09 -->

<!-- iris:object Q10 kind=issue owner=M04 -->

### Q10 模型协议字段丢失与续轮保真

- **已确认事实**：DeepSeek 推理续接字段在部分 assistant 消息中被丢弃；DeepSeek 的 `extra_body` SDK 参数与实际 HTTP 请求字段存在混淆；Responses 续轮时稳定指令和新追加的修复消息可能没有进入最终请求；服务端搜索事件和引用尚未完整进入现有网关处理路径。
- **不能推出**：某一协议的问题推成所有模型均受影响；也不能用「换个更强模型是否能成功」的实验来合理化已知合同错误。
- **影响边界**：`C11`、`C12`、`M04`。
- **所需证据**：按协议合同的字段保真测试与出站结构证据（`V08`）。**本条的关闭证据是事实 1–3 的机械记录（`V03`）**，见 `registry.json.verify`；逐端点 `V05` 是验证目标但**不是关闭门槛**，由 `G03`／`Q17`／`D04` 承接（2026-09-17 关闭门槛改写）。
- **覆盖**：DeepSeek 直发 HTTP 使用顶层 `thinking`（不再写 `extra_body`）；同一 Provider 工具续轮 assistant 回放 `reasoning_content`，普通历史不回放私有推理；Responses 续轮保留稳定 `instructions` 并将 Host System 修复写入 `input`。服务端搜索事件／引用不在本条范围（→ `G03`）。`V03` 机械记录见 `registry.json.verify`；不把 `Q10`／`C11` 的 `verification.state` 标为通过。本条由 `D02` 验收关闭，关闭只表示事实 1–3 已修复并有机械覆盖，**不表示任一端点已 live 通过**，也不关闭 `G03`／`Q17`。

<!-- iris:end Q10 -->

<!-- iris:object Q11 kind=issue owner=E03 -->

### Q11 部分确定性测试与实现共享同一错误假设

- **已确认事实**：本轮复核中模型网关曾有把有问题的请求结构当作正确期望的测试；部分连续性压力测试使用替身模型只返回确认文本，最终检查的是关键约束是否出现在历史或摘要中。确定性评测现以 `evidenceLevel`／`claimBoundary`／`semanticStatus` 分列，机械 smoke 不得把语义或 live 标为通过；MiniMax 标准 `tool_calls` 不得再被标成内容抽取。
- **不能推出**：「机械测试过了即完成」或「语义质量自动评价没用」的绝对说法。连续性替身仍只证明约束出现，不证明语义质量（`V06`）。
- **影响边界**：`E01`–`E03`、`V01`–`V07`。
- **所需证据**：确定性合同、组合轨迹与真实任务评价三类证据分开报告（`V01`），负例测试必须在正确实现中成功发现违规。
- **覆盖**：v3 summary 分列证据用途；负例拒绝无 baselineIdentity、脏树 comparable、零分母满分，以及机械报告冒充语义／live 通过。

<!-- iris:end Q11 -->

<!-- iris:object Q12 kind=issue owner=M09 -->

### Q12 历史诊断不足以区分工具名来源

- **已确认事实**：工具名可能来自模型生成、协议解析、名称映射或提示约定冲突，四处都会产生相同现象。C11／C17／C27 现记录并解释这四类来源；缺解析 hop 时不得归因于模型。
- **不能推出**：在缺少关联记录时把现象直接归因于模型。
- **影响边界**：`C11`、`C16`、`C17`、`C26`。
- **所需证据**：与 `Q01` 同一组关联记录；在此之前不得归因于模型。
- **覆盖**：现行来源链与 C27 解释已接入；历史未知工具提议仍不得事后唯一归因于模型（见 `Q01`）。

<!-- iris:end Q12 -->

<!-- iris:object Q13 kind=issue owner=M05 -->

### Q13 预算存档兼容：同版本不同数值可能读取失败

- **已确认事实**：`agent_run_repository.rs` 的 `materialize_budget_policy` 会比较存档策略与规范策略，同版本但数值不匹配可能返回 `InvalidBudgetPolicy`；当前没有通用的 schema 3 旧数值兼容分支。
- **不能推出**：必须采用唯一迁移实现；也不等同于 SQLite 表结构迁移。`R13` 已采用不等于本条关闭，也不等于 `D02` 可验收。
- **影响边界**：`C13`、`M01`、`K06`。
- **所需证据**：第一次修改 `for_profile` 默认值时，必须升 `schema_version`、冻结上一版规范值，并使旧 Run 可读且不扩权（`V04`）。当前不改数，不要求常量表落地。
- **实测（2026-09-17）**：把本地上限与网络上限各收紧一档的同版本存档会被拒绝为 `Run(InvalidBudgetPolicy)`——即同版本任何数值差异都不可读。在同一 schema 内放宽为「取更严格一侧」的尝试已被否证：它会让`complete_but_noncanonical_budget_policies_fail_closed_for_read_and_retry` 的放宽用例不再失败关闭，等于取消篡改检测。门禁见已采用的 `R13`。
- **覆盖**：本条仍 open；已移出 `D02` 的 `scope` 与 `blocks D02@acceptance`。第一次改默认值的施工被本条挡住，届时再挂阻断边。
- **未确定**：第一次改数时的具体 `schema_version` 与冻结构落点；被篡改的下调与历史下调在读取时不可区分（下调只收紧不扩权，故接受）；`legacy_budget_drift` 是否需要用户可见。

<!-- iris:end Q13 -->

<!-- iris:object Q14 kind=issue owner=M07 -->

### Q14 双路搜索对纯编辑任务的触发范围未单独确认

- **已确认事实**：一次执行新网页搜索即触发双路合同；纯润色、提供材料内的格式整理不因联网开关打开而自动搜索。
- **不能推出**：把双路要求退回「两路可选」。时间上是否并行是执行策略（默认并发、受约束时串行），两路实际执行是功能合同。
- **影响边界**：`C20`、`L02`、`L03`。
- **所需证据**：分别验证「需要联网材料的组合任务」与「纯编辑任务不触发检索」。

<!-- iris:end Q14 -->

<!-- iris:object Q15 kind=issue owner=M03 -->

### Q15 长期记忆的写入时机、有效期、冲突与用户管理未确定

- **已确认事实**：当前存在独立存储与读写工具（`memory_read`／`memory_write`）；具体产品行为未确定。
- **已确定的语义（2026-09-19）**：写入只能由 `memory_write` 显式发起，该工具 `requires_confirmation = true` 且 `default_enabled_without_skill = false`——**默认不把任意聊天内容自动写成长期事实**；`ai_memories` 在全仓只有这一条写入路径（`tool_dispatch/memory.rs`），会话压缩与运行引擎都不写它。作用域由入参 `scope` 决定：`global` 与 `vault`（`vault` 需有活动 Vault，否则 `memory_vault_scope_unavailable`）；`clear_scope` 只清自身作用域，不越界；迁移 `071` 使同 key 在不同作用域互不冲突。
- **明确不实现（本条关闭后仍不承诺）**：条目**有效期**与**冲突呈现**。表结构没有过期列，也没有面向用户的冲突裁决界面。把这两项写成已实现是错的；它们需要单独的功能决定与工作包。
- **不能推出**：把尚未确定的交互写成已实现能力；也不能因为本条关闭就宣布「长期记忆的产品行为已完整确定」。
- **影响边界**：`C09`、`K08`、`T13`、`T14`。
- **所需证据**：确认合同下的读写、作用域与失效表达；默认不把任意聊天内容自动写成长期事实。
- **覆盖**：`V03` 机械记录见 `registry.json.verify`；本条由 `D03`／`D05` 的验收不再需要它阻断（`blocks` 边与 `scope` 一并移出），关闭依据是上列已确定语义与「明确不实现」的边界。

<!-- iris:end Q15 -->

<!-- iris:object Q16 kind=issue owner=M01 -->

### Q16 运行中追加／修改指令的交互与状态处理未确定

- **已确认事实**：架构已确定「输入修订在安全点生效」的方向。**交互形态于 2026-09-19 确定**：Host 在 Run 进行中拒绝新的用户指令，返回稳定错误码 `agent_run_active_run_exists`，界面文案为「当前会话已有任务运行，请等待、取消或完成后重试」（`normal_session_repository.rs` 的 `ensure_session_idle`、`agent_run_repository.rs` 的 `ensure_no_active_top_level_run`、`useUnifiedAssistantSend.ts` 的 `normalSubmissionError`）。用户要追加或修改要求，只能等待、显式取消或在完成后重发。
- **不能推出**：把安全点吸收写成已经实现的端到端交互；也不能把「取消后留下的 interrupted partial」读成「本 Run 已发布答案」——它只是下一轮的上下文痕迹，Run 终态仍是 `Cancelled` 且无 `final_message_id`。
- **影响边界**：`C03`、`K03`、`C25`。
- **所需证据**：旧修订未发布答案不能覆盖新要求；取消后不启动新副作用（由 `D03` 承接）。
- **覆盖**：两条性质各有绑定证据——`cancellation_after_candidate_but_before_commit_cannot_publish_it`（候选产生后取消不得发布）、`cancelled_run_never_starts_a_model_or_tool_turn`／`cancellation_after_the_first_dispatch_opens_no_further_turn`（取消后不再开轮）、`cancelled_run_never_commits_a_markdown_patch`（取消不提交写盘）。形态与性质写进 [`K03`](../contracts/K03-instruction-revision.md) §一。**本条不承诺**运行中就地修订通道、修订队列或相应 UI：要做到那些必须先确定其交互形态并作为新决定登记。
- **归属**：2026-09-16 经治理修复由 `D02` 迁入 `D03`；2026-09-19 形态确定后本条关闭。关闭是「选择已作出」，不是「运行中修订已实现」。

<!-- iris:end Q16 -->

<!-- iris:object Q17 kind=issue owner=M04 -->

### Q17 原生搜索端点、混合续轮、费用与权限适配待验证

- **已确认事实**：不同厂商文档的能力表述不一致；`OpenAI-compatible` 标签或首轮文本成功不足以证明工具声明、客户端工具续轮与返回事件合同成立。本体系**不宣布** MiniMax、DeepSeek 或 Gemini 任一端点已通过。
- **不能推出**：仅凭厂商名或品牌判定能力可用。
- **影响边界**：`C10`、`C11`、`C20`、`K12`。
- **所需证据**：逐端点的协议测试加对应端点真实调用（`V05`）。本条是 `V05` 的**承接方**：2026-09-17 关闭门槛改写把协议侧的逐端点 `V05` 从 `Q04`／`Q10` 移到这里与 `G03`／`D04`，本条与 `G03` 仍 open，`D04` 关闭它们。
- **归属**：`Q04`／`Q10` 的关闭不依赖本条；本条不被 `D02` 关闭。`D02` 只交付机械续轮与终止原因，其 §三「关闭门槛」表已写明。
- **授权前置（2026-09-19 登记）**：`V05` 的窗口、待验证端点矩阵、逐端点检查项与需要用户批准的四件事（端点范围、费用上限、凭据、原生搜索端点）见 [2026-09-19 V05 逐端点实网验证窗口](../../docs/eval/results/2026-09-19-v05-live-window.md)。**未获批准前一条也不运行**，本条保持 `open`。
  该窗口同时登记一个必须说清的事实：当前生产目录的 `ResponsesReserved` 只有 mimo 的四个 ASR／TTS 模型且 `supports_tools = false`，因此**没有支持工具的 Responses 端点**，「Responses 续轮保真」的逐端点 live 验证在本目录下无法进行，只能在机械层（已有 `V03`）与接入真实端点后补。

<!-- iris:end Q17 -->

<!-- iris:object Q18 kind=issue owner=M09 -->

### Q18 缺少出站请求的安全结构证据

- **已确认事实**：只有哈希不足以解释字段缺失。`C26` 已记录消息角色与数量、必要字段是否存在、修复消息是否纳入、所用协议，以及前后边界的关联标识；见证不含笔记正文、密钥或私有推理。`C27` 呈现这些事实，`V07` 八类故障投影检查定位与脱敏。
- **不能推出**：出站记录能证明语义理解。
- **影响边界**：`C26`、`C11`、`K17`。
- **所需证据**：与 Run、模型轮次、尝试、工具调用及父子任务建立联系，并遵守最小化与脱敏约束（`V08`）。
- **覆盖**：结构见证、关联键与 C27／V07 呈现已接入。

<!-- iris:end Q18 -->

<!-- iris:object Q19 kind=issue owner=M05 -->

### Q19 网络额度是否为主要瓶颈待验证

- **已确认事实**：Standard／Delegated／DurableApply 的 `max_network_tool_calls` 都是 6，这是**主循环 Network 类工具额度**；不是全部服务商请求、页面或父子任务网络活动的统一总数。`web_search`、`web_fetch` 共用该类别额度；`web_fetch` 接收 `urls` 数组，主路径按待抓 URL 数计算单次上限，最多 8。
- **不能推出**：最多抓四篇／五篇；非网页派发上下文中的 `max_web_fetches: 5` 不属于网页主链路全局上限。
- **影响边界**：`C13`、`K06`、`Q13`。
- **所需证据**：结合实际派发、成功观察、失败恢复与剩余预算判断；六次偏紧是待验证风险，不是已确认的统一根因。

<!-- iris:end Q19 -->

<!-- iris:object P04 kind=rules -->

## 基线对象要点

- **范围**：记录基线提交、源码事实与 `Q*` 未决事项；不声明修复完成，不把历史故障归因成唯一根因。
- **事实来源**：架构定义基线 `2670739f`、讨论纪要首轮 `b9198619` 与两轮复核 `2d357038`。
- **边界**：每条 `Q*` 注明已确认事实、不能推出的结论、影响边界与所需证据。
- **规则**：只登记能在注明基线上核对的事实；无法核对的写「未知」并登记缺口；历史观察不得改写成当前承诺。

## 异常处理

发现基线事实与源码不符时登记新的 `Q*`，不改写旧结论；来源对象指纹变化时本文件进入复核。

<!-- iris:end P04 -->
