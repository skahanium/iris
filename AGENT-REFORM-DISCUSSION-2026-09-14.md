# Iris 下一阶段 Agent 革新：讨论纪要与判断依据

讨论日期：2026 年 9 月 14—15 日

整理日期：2026 年 9 月 15 日

文档性质：产品需求、审查事实与技术取舍的讨论记录；不构成实施方案、施工计划或发布验收结论。

后续架构定义见 [Iris Agent 架构定义](./docs/agent-architecture.md)。该文档记录用户委托 Codex 于 2026-09-15 明确的模块、组件、工具与交接设计；本纪要保留此前讨论及事实边界，不将后续设计追写为当时已经确认的结论。

本文整理用户与 Codex 的讨论，以及 DeepSeek（批注署名 DSH）的两轮复核和后续订正。各节交代问题从何而来、比较过哪些选择、观点如何变化、目前保留什么判断。第一、二节先说明产品目的，此后从运行边界、协议与部件展开，最后讨论子 agent 和框架取舍。

**阅读与引用规则：**

- **明确要求**：用户直接提出或纠正过的需求；不能被助手的偏好替代。
- **已核实事实**：在注明的代码基线或历史记录中观察到的行为；不自动构成根因、产品效果或当前版本承诺。
- **当前建议**：Codex 或 DeepSeek 的判断，允许被后续证据推翻；不等同于用户已批准的方案。
- **待验证／待讨论**：尚缺运行证据、比较结果或产品边界定义。
- 已撤回的观点只保留在各节的讨论过程及文末历史表中，均明确注明失效。第十八节汇总有效状态；不能摘取旧观点作为当前决策依据。

**事实基线与文档边界：**

| 范围               | 基线与含义                                                                                        |
| ------------------ | ------------------------------------------------------------------------------------------------- |
| 首轮审查           | `b9198619` 及 2026-09-14 当时工作树；期间存在并行修改，未提交代码中的新问题不能倒推为此前故障原因 |
| 两轮批注与复核     | `2d357038`，2026-09-15；正文单独说明与首轮有关的变更，不把局部变更视为全部问题已解决              |
| 历史故障           | 2026-09-14 的开发版运行记录；记录不足时保留未知原因，不伪造完整请求轨迹                           |
| 外部框架与接口资料 | 讨论时查阅的资料，仅支持当时讨论；端点、模型版本、费用及实际混合调用兼容仍需针对目标配置验证      |
| 本次整理           | 合并已讨论的事实与订正，不新增实网实验结果，不声明代码问题已修复                                  |

版本与排期以 [ROADMAP.md](./ROADMAP.md) 为准；实现边界结合 [ARCHITECTURE.md](./ARCHITECTURE.md) 与对应代码核对。原 Agent Harness 建设文档体系已于 2026-09-15 整体归档，仅供历史对照；归档入口见 [文档索引](./docs/README.md) 的“归档”部分。新的 Harness 文档入口及阶段状态归属尚未确定，本文仍是讨论依据，不因旧体系归档而成为新规范或路线图。源码链接使用仓库相对路径，关键符号用于定位；历史事实还需结合上述提交基线理解。

目前最重要的状态是：基础多轮对话与文档任务必须可靠；可用且获准时，原生与 MCP 两路搜索都实际执行；预算瓶颈尚未证实；历史未知工具故障的唯一根因尚未确定；优先保留运行底座、处理薄弱环节仍是助手建议，具体替代框架尚未选定。

---

## 一、讨论从哪里开始：基本对话不可靠，引出了基座替换问题

最初的问题是：Iris 的 agent 内核一直难以做好，是否可以直接使用 Mirascope、Vercel AI SDK 的 `ToolLoopAgent` 等现成实现替换。随后，候选范围扩大到 PydanticAI、smolagents 及其他可能适合的实现。

用户很快指出，仅凭框架功能比较，无法得到可信答案。必须先理解 Iris 的项目目的、实际需求和边界。

真正改变讨论方向的是用户提供的使用经历：

> 在源码启动的开发版中，先打招呼、询问时间，再问近期上映或即将上映的电影，就可能得到偏题、小众甚至错误的回答；继续追问又容易出现模型能力或联网降级提示。

当时使用的是官方 MiniMax-M3，以及 AnySearch、Tavily 的 MCP 服务。用户也明确说明，尚未用原本的 Iris 做过深入研究，因为简单连续对话就没有达到可用程度。

这意味着，当前首要问题不能被定义为“缺少高级研究能力”。**基本对话、时效检索和追问纠正是否可靠，才是判断 agent 基础是否成立的起点。**

讨论由此从“哪个框架功能更多”转向：

- 当前行为究竟在哪个环节失真？
- 哪些问题来自模型、工具服务，哪些来自 Iris 自己？
- 哪些基础值得保留，哪些实现需要替换？
- 现有测试为什么没有充分暴露这些问题？

**目前结论：**框架选型必须服从实际任务和故障证据，不能用框架名称替代问题诊断。

---

## 二、产品目的与实际需求：Iris 需要什么样的 agent

用户提出的需求涵盖自然对话、联网信息获取，以及围绕 Markdown 文档的辅助工作。其中，编辑相关需求最明确。

| 需求               | 用户的具体描述                                                                   | 对 agent 能力的要求                          |
| ------------------ | -------------------------------------------------------------------------------- | -------------------------------------------- |
| 润色与扩缩写       | 选中文字自动进入引用，在对话框中要求扩写、缩写、润色；对象可能是字词、单句或一段 | 准确绑定选区、理解偏好、控制修改范围         |
| 生成新内容         | 根据要求和目标文件的格式、文风，生成原本不存在的句子或段落，由用户插入           | 获取必要文风样例、理解插入目的、形成候选文本 |
| Markdown 格式整理  | 将复制来的非 Markdown 内容整理为标准 Markdown，不修改实际内容，序号除外          | 内容保持、结构转换、差异检查                 |
| 自然连续对话       | 能承接前文、理解指代、切换话题、接受纠正                                         | 稳定的历史与上下文处理                       |
| 联网查询与后续核实 | 能回答当前信息问题，追问后继续核查                                               | 搜索、阅读、证据判断和纠错                   |
| 长对话             | 超过上下文范围后仍能保留重要约束和任务状态                                       | 裁剪、压缩、记忆与恢复                       |

由这些需求，讨论形成了一个重要判断：**不同任务可以共享运行底座，但不应强制经过同样复杂的执行过程。**

润色一小段文字通常不需要研究循环或子 agent；整理 Markdown 更依赖内容差异检查；联网核实则需要真实的检索反馈和继续行动能力。

同时，“生成候选文本”和“实际修改文件”是不同效果。用户对新内容的描述是由用户插入，不能被自动扩张为任意文件写入授权。

**明确要求：**先确保上述真实场景可用，尤其是内容保持、文风适配、自然追问和纠正。

**尚未锁定：**所有编辑动作的最终交互形态、运行中补充要求的具体操作方式，以及各种任务的自动化程度。

---

## 三、现有技术与数据边界：哪些取舍不能只看框架功能

Iris 的现行项目约束包括 Tauri 2、Rust 后端、React 19、TipTap，以及 SQLite + sqlite-vec；新增依赖需要与 AGPL-3.0 兼容。Markdown 文件是笔记内容的权威来源，实际修改需要遵守用户授权、目标绑定和版本校验。

这些约束对 agent 革新有直接影响：

- Python 或 TypeScript 框架的能力可以作为参考，但接入方式必须符合项目运行边界。
- 引入另一个后端运行时，属于架构变化，不能被称为“直接换一个库”。
- 不能为了使用某个 SDK，把凭证、模型请求或文件副作用随意移到前端。
- 框架不能取代 Iris 对文件权限、确认和数据完整性的责任。

相关依据见 [项目开发规范](./AGENTS.md) 与 [架构说明](./ARCHITECTURE.md)。

**目前结论：**讨论中没有形成放宽现行技术栈和数据边界的决定。后续比较必须将这些视为实际约束。

---

## 四、先统一概念：模型、agent 内核、harness、MCP 各是什么

为了回答“到底替换什么”，讨论双方先梳理了部件职责。

一个最小可工作的 agent，至少需要：

1. 模型及模型接口。
2. 上下文与指令组装。
3. 决策和执行循环。
4. 工具描述、校验、执行与结果返回。
5. 完成、澄清、失败和结果发布处理。

产品化后，还需要会话与运行状态、权限、记忆、检索、证据、诊断和评测。

讨论中进一步区分了几个容易混用的概念：

| 概念           | 在本轮讨论中的含义                                   |
| -------------- | ---------------------------------------------------- |
| 模型           | 提供理解、生成和行动选择能力                         |
| Agent 内核     | 组织上下文、模型调用、工具执行和反馈循环             |
| 运行时 harness | 为执行提供权限、预算、状态、上下文、恢复与结果约束   |
| 评测 harness   | 运行测试场景，收集轨迹，评价结果                     |
| MCP            | 接入外部工具的协议，不负责理解任务或判断证据是否充分 |
| Skills         | 可复用的任务方法和知识，不自动增加权限               |
| 子 agent       | 在受限上下文中执行独立子任务的可选机制               |

首轮审查时，Iris 的 `agent-harness/` 曾是建设文档入口；该文档体系已于 2026-09-15 整体归档，替代入口尚未确定，历史材料可经 [文档索引](./docs/README.md) 查阅。文档归档本身不表示生产运行实现已退役；本轮审查涉及的实现主要位于 `src-tauri/src/ai_runtime/`。运行时 harness 与评测 harness 的职责区分仍然成立，不能把 harness 简化为一个测试目录。

**目前结论：**更换工具循环不等于替换完整 harness；增加 MCP、记忆或子 agent，也不等于提升了基本 agent 质量。

持久化也需要区分含义：保存会话、显示 Run 事件，不等于重启后能够恢复任意中断中的执行。首轮审查中，普通 Direct／ToolLoop 的进程级续跑与受限 Durable Apply 的检查点恢复是不同能力，不能合并宣传为通用持久 agent。

---

## 五、现有基础审查：Iris 不是缺少多数模块，而是连接不稳

审查确认，Iris 已经具备相当多的基础实现：

| 已有基础                   | 实际价值                 | 仍不能据此证明的能力   |
| -------------------------- | ------------------------ | ---------------------- |
| Run 生命周期、持久化与事件 | 记录运行和控制状态       | 用户任务已经完成       |
| 统一工具循环               | 管理工具、模型续轮和预算 | 搜索与纠错策略有效     |
| 工具目录与权限             | 限制可调用动作和副作用   | 模型能稳定遵守工具合同 |
| 本地及网络检索 Broker      | 统一执行与结果处理       | 检索内容相关、覆盖充分 |
| 证据账本与引用             | 管理来源身份             | 来源真正支持结论       |
| 会话历史与摘要             | 控制上下文大小           | 压缩后仍正确理解前文   |
| Skills 与 ChildRun         | 提供扩展和委派路径       | 复杂分工已经成熟       |
| 确定性测试及真实评测机制   | 验证部分合同和产品表现   | 所有自然场景都已验证   |

因此，Codex给出的总体判断是：

> Iris 较完整地建设了“管理一次运行”的基础，但“理解任务、正确使用工具、根据反馈纠错、完成用户目标”这条链路仍不稳定。

这个判断来自实际发现，而非仅凭架构风格作出。

**当次审查中最重要的证据如下：**

| 发现                                                             | 可以支持的判断                       | 不能扩大成的结论                          |
| ---------------------------------------------------------------- | ------------------------------------ | ----------------------------------------- |
| 历史运行出现搜索和抓取均成功，随后连续未知工具提议，最终恢复耗尽 | 至少一次失败发生在工具提议与恢复链路 | 不能认定所有问题都是 MCP，或全部都是模型  |
| 有历史消息时，泛问起步查询不再满足自动补充“中国大陆”的条件       | 寒暄会改变初始搜索处理               | 不证明一定得到错误结果                    |
| 部分模型必要字段在消息转换或续轮中丢失                           | 模型适配存在明确合同问题             | 不直接证明它导致了 MiniMax 的那次历史故障 |
| 多种失败可能汇聚为来源关联限制说明                               | 用户看到的原因可能不够准确           | 不等于联网服务真的失败                    |
| token 裁剪与按消息条数推进的压缩边界不同                         | 存在历史覆盖缺口风险                 | 不能说系统完全没有记忆或缺口检测          |
| 新工作树预算投影可能因一个类别耗尽而要求整体收束                 | 状态简化可能改变循环行为             | 不能倒推为旧版本故障原因                  |

其中，9 月 14 日 09:40 的记录尤其重要：初始搜索和抓取成功后，模型连续两轮提出目录外工具，最终进入 `recovery_exhausted`。

目前能确定直接失败位置，但历史诊断不足以区分工具名来自模型生成、协议解析、名称映射还是提示约定冲突。

**后续争议及订正：**DeepSeek 第一轮批注曾把网络预算耗尽加入这次历史故障的解释。复核发现，该记录在三轮模型调用、两次工具调用后退出；旧版及复核版都有“连续两轮提议全部未派发后收束”的独立规则。未派发的提议不按实际执行路径增加网络工具计数。因此，这条记录支持“修复阈值触发”，不支持“六次网络额度用完”。DeepSeek 第二轮撤回了该因果推论。

恢复阈值是否合理与未知工具为何产生，是两个不同问题；把网络额度调高不会自动解除两轮拒绝收束规则。原表中“不把新工作树问题倒推为历史故障原因”的限制继续有效。依据：[AgentToolLoop](./src-tauri/src/ai_runtime/agent_tool_loop.rs) 中提议派发、`rejected_rounds` 和退出诊断分支。

**截至复核基线的变化：**`3a8c1903` 将 Host 联网保底从模型行动前延后到终局校验边界，让模型先理解问题并选择查询。这改变了旧的起步执行顺序，但没有证明电影推荐或地域相关问题已在真实模型上稳定解决。`bootstrap_web_query` 的条件仍需结合保底触发路径理解。

DeepSeek 第一轮把 `154d7f12`、`f0548246` 当成网页地域规则修复依据，第二轮已撤回。二者实际处理本地 FTS 查询、分数与路径范围语义，不能作为“中国大陆”搜索默认规则已修复的证据。依据：[Run 上下文](./src-tauri/src/ai_runtime/run_context.rs)、[本地 FTS](./src-tauri/src/ai_runtime/retrieval_broker/fts.rs) 及上述提交。

**目前结论：**已经找到了多个可以定位的问题，但仍不能把用户经历压缩成一个未经验证的“唯一根因”。

---

## 六、模型与协议适配：从“能调用”转向“能完整续轮”

用户明确表示，MiniMax、DeepSeek、Gemini Flash 可能成为长期主模型，前两者使用更多，同时希望尽量多适配国产模型。

用户还提出，是否需要适配 OpenAI、Anthropic 等不同 API 格式。讨论中需要澄清：OpenAI Chat Completions、OpenAI Responses、Anthropic Messages 是不同协议形态，不能统称为同一种 Responses 格式。

比较过的思路包括：

- 尽量统一走 OpenAI-compatible 格式。
- 为不同协议和供应商保留明确适配。
- 采用成熟实现承担部分协议维护。
- 继续全部自研。

审查使“只要接口兼容就足够”的判断站不住脚。

当次发现包括：

- DeepSeek 推理续接字段在部分 assistant 消息中被丢弃。
- DeepSeek 的 `extra_body` SDK 参数与实际 HTTP 请求字段存在混淆。
- Responses 续轮时，稳定指令和新追加的修复消息可能没有进入最终请求。
- 服务端搜索事件和引用尚未完整进入现有网关处理路径。

这些问题说明，**第一轮能够返回文本，不等于 agent 适配完成**。完整适配还涉及工具调用、流式解析、续轮、取消、错误分类、能力边界和来源信息。

**明确要求：**广泛适配国产模型，以 MiniMax、DeepSeek 为重要验证对象。

**当前建议：**模型协议层是最值得评估成熟实现替代自研代码的部分。

**待验证事项：**具体依赖、支持端点、模型版本及各协议能力矩阵；本轮没有选定替代库。

**后续补充：**DeepSeek 指出，双路搜索汇总依赖原生事件、URL 和引用能够完整进入执行链；LOOP 的修复反馈依赖消息确实进入最终请求。这个依赖判断成立，但不等于所有设计讨论都必须暂停，也不能把某一协议的问题推成所有模型均受影响。

已确认的字段丢失应按对应协议合同处理，不必等待“更强模型是否能成功”的实验才承认其问题。对照实验用于评估影响和优先级，不能把已知合同错误合理化。相关实现见 [消息转换](./src-tauri/src/ai_runtime/model_gateway/messages.rs)、[请求构造](./src-tauri/src/ai_runtime/model_gateway/body.rs)。

原生搜索尤其不能只按厂商名判断“有／没有”。本轮查阅的 DeepSeek 不同接口文档存在能力表述不一致，MiniMax 的服务端搜索资料也不能替代 Iris 的实际接入验证。具体模型、端点、协议、工具声明、客户端工具续轮及返回事件共同决定可用能力。“OpenAI-compatible”标签或首轮文本成功不足以证明这些合同成立。

---

## 七、工具与模块职责：明确谁记录事实、谁作判断

用户追问如何收清模块职责，这将讨论从“分多少模块”推进到了“模块之间应如何交接”。

讨论双方提出，每个模块应明确：

- 接受哪些输入。
- 交付哪些输出。
- 拥有哪些决定权。
- 必须保持哪些事实不变。

形成的职责方向是：

| 模块       | 应承担的职责                               |
| ---------- | ------------------------------------------ |
| 请求和权限 | 固定授权、范围、目标及副作用边界           |
| 上下文     | 选择材料，说明覆盖、裁剪和压缩情况         |
| 模型网关   | 保持协议与续轮信息正确                     |
| 工具执行   | 如实报告派发、成功、失败和部分结果         |
| 检索与证据 | 管理线索、正文、来源、时间和阅读范围       |
| 模型       | 理解问题，判断相关性和语义缺口，选择下一步 |
| 循环控制   | 管理预算、取消、恢复及执行状态             |
| 最终发布   | 准确表达答案、部分完成、受阻或失败         |

讨论中的核心原则是：**一种执行事实只能有一个权威来源，后续投影不能改写它。**

例如：

- 抓取超时不能在压缩后变成成功。
- 搜索结果不能仅因为进入上下文就升级为已核实证据。
- 恢复耗尽不能直接解释成模型能力不足。
- 来源绑定失败不能替代其他失败的真实原因。

**当前建议：**职责清理应围绕事实和决定权展开，而不以拆文件、增加抽象层为成果。

故障归属应按交接边界判断。例如，模型提议工具名、网关解析后的工具名、目录接受的工具及真实派发结果是不同事实；只有前后证据能够关联，才可以判断问题发生在哪里。工具服务返回成功也不表示信息相关，语义验证不能由工具状态代替。

Skills 和子 agent 同样遵守这些边界：方法指导不增加权限，委派不绕过联网或文件限制，子任务报告不能自行把候选线索升级为已核实结论。

---

## 八、联网能力：从单渠道选择，明确演变为双路实际搜索

这是本轮经过用户多次纠正后，最明确的一项产品要求。

讨论经历了以下变化：

1. 用户指出 MCP 搜索链路不够可靠，希望利用模型 API 原生搜索。
2. 用户同时强调，严格场景需要保留联网限制。
3. Codex最初提出保留开关，在不同搜索渠道之间自动选择。
4. 用户明确提出，API 原生搜索与 MCP 搜索应当同时使用。
5. Codex又区分了“两边可用”和“两边必用”，倾向默认由模型选择。
6. 用户再次明确：**用户要的是两边都实际使用。**
7. 用户进一步纠正，MCP 并非仅补充抓取，它本身也必须搜索。

由此得到的明确结构是：

```text
API 原生搜索 ─┐
              ├→ 汇总两边结果、去重、选择需要深入阅读的 URL
MCP 搜索 ─────┘                         ↓
                                MCP 抓取／正文提取
                                         ↓
                                  模型综合与回答
```

**明确要求：**

- 支持原生搜索且 MCP 可用时，两条搜索链路都实际执行。
- 不能仅开放两个工具，然后把是否调用两边交给模型自由决定。
- MCP 自身承担独立搜索。
- 两边发现的网页都可以交给 MCP 抓取。
- 最终综合双方实际取得的材料，不凭空凑出“双方一致”。

还需要保持两个区分：

- 两个渠道找到同一篇报道，不等于获得两个独立来源。
- 两路实际执行，不等于必须取得两个成功结果；失败应单独记录，不能无限重试或假装完成。

**尚未单独界定：**这一要求与纯润色等无需外部资料的任务如何配合，以及“同时”是否还要求请求在时间上并行。这些不应被自行推定，但也不能据此把双路实际执行退回“两路可选”。

**DeepSeek 批注带来的补充与修正：**

- 第一轮曾以“原生搜索一次、MCP 搜索一次、每篇抓取一次”推导最多只能抓四篇，进而认为双路需求在当前预算下无法实现。第二轮已撤回：一次 `web_fetch` 可处理多个 URL，且需求是按需阅读，不是抓取全部搜索结果。正确预算口径见第十一节。
- MCP 链路效果不佳是用户反馈，但还不能据此认定搜索服务自身召回不足。查询生成、服务端结果、Iris 的候选截取与模型选择都可能影响最终覆盖。双路搜索是明确产品需求，其增益大小仍需比较，不能用“尚未被推翻”充当已验证事实。
- 两个检索渠道可能增加有效线索，但也可能返回重复材料。不能承诺固定程度的召回提升。
- 两路成本与延迟不必翻倍。费用取决于价格、请求与内容用量；时间取决于并行或串行、较慢链路、重试及后续抓取。额外成本值得记录，但本轮没有确定费用上限或延迟目标。

**来源冲突的当前建议：**两路返回不同事实时，不预设原生或 MCP 优先。比较原始出处、事件时间、地域、定义和统计口径，再判断正文支持程度；必要时补查。无法消除的冲突应如实呈现。渠道数量、转载数量和独立证据数量不能混算。具体自动判断方式尚未确定。

---

## 九、搜索与抓取：不同能力可以交叉组合

用户进一步追问，原生 API 是否只有搜索，而 AnySearch、Tavily 同时具有网页抓取能力。

讨论区分了三种能力：

1. 根据关键词发现网页。
2. 服务商在搜索过程中读取或提取内容。
3. Iris 指定一个 URL，独立取得页面正文。

讨论时查阅的 MiniMax 公开 Server Tools 以 `web_search` 为主。它可能返回内容和引用，但不能据此认为提供了任意 URL 的独立完整抓取接口。[MiniMax Server Tools](https://platform.minimax.io/docs/guides/server-tools)

AnySearch 和 Tavily 则明确提供搜索与提取：

- AnySearch：`search`、`extract`。
- Tavily：Search、Extract；Search 还可以按参数返回提取内容。

依据：[AnySearch 官方仓库](https://github.com/anysearch-ai/anysearch-mcp-server)、[Tavily Extract](https://docs.tavily.com/documentation/api-reference/endpoint/extract)。

Iris 当前预设也已经包含上述搜索与抓取映射。[MCP 服务预设](./src/components/ai/skills/mcpProviderPresets.ts)

**明确要求：**抓取渠道不必与发现网页的渠道一致。原生搜索返回的 URL，可以直接交给 MCP 提取，不必让 MCP 再搜索同一个地址。

**当前判断：**原生搜索返回的 URL、出处与引用需要进入 Iris 可追踪的来源处理链，以便后续选择和抓取。当前抓取安全边界已允许直接输入符合条件的公开 HTTPS URL，并不要求它先出现在本 Run 的搜索候选集中；统一记录来源不应重新引入“先搜索才能抓取”的限制。依据：[网页执行与 URL 校验](./src-tauri/src/ai_runtime/run_tool_loop.rs) 中 `validate_public_fetch_urls` 与 `execute_web_tool` 相关路径。

**边界：**只有生成文本、没有可靠来源地址时，不能把其中任何看似 URL 的字符串自动当作已发现来源；有提取内容也不能自动宣称完整网页已读完。

---

## 十、联网开关与兼容：增强能力不能改变授权含义

用户主动设置联网开关，是因为某些场景必须严格限制搜索。随后讨论了三种选择：

- 去掉开关，完全让模型决定。
- 保留开关，但只控制 MCP。
- 保留开关，统一控制所有搜索与网页读取路径。

Codex建议第三种，用户后续讨论也继续以“开关开启时双路执行”为前提。

形成的方向是：

- 联网授权覆盖原生搜索、MCP 搜索及网页抓取。
- 子 agent、重试和模型切换不能绕过授权。
- 开启代表允许使用外部网页信息，不等于材料和数据外发限制全部解除。
- 网页检索关闭与完全离线不同：云端模型请求仍然涉及数据外发。

还讨论了原生搜索的特殊边界：它在供应商服务端执行，Iris 未必能在搜索前检查查询。因此，禁止笔记内容进入搜索引擎的任务，需要选择能控制外发查询的路径，或把允许公开的信息单独送入搜索请求。

这属于严格场景的能力约束，不能仅靠一句提示词完成。

用户随后明确要求兼容不支持原生搜索的模型，并且降级应稳健、无感。

**明确要求：**

| 状况                  | 行为                                    |
| --------------------- | --------------------------------------- |
| 原生搜索与 MCP 均可用 | 两路实际搜索，共享后续抓取              |
| 模型不支持原生搜索    | 正常使用 MCP 搜索与抓取，不显示能力降级 |
| 原生搜索临时失败      | 保留 MCP 已取得结果，继续判断能否完成   |
| MCP 搜索失败          | 保留原生结果；MCP 抓取是否可用单独判断  |
| 所有可用路径失败      | 如实说明无法完成当前核实                |
| 联网关闭              | 禁用新的网页搜索和抓取                  |

这里的“无感”有明确边界：**路径变化不影响任务完成时，不打扰用户；确实导致证据不足时，不能隐藏限制。**

“不支持原生搜索”是正常能力配置，“原来可用但本次超时或失败”是运行故障，二者的诊断含义不同。搜索失败也不能直接推断同服务的抓取失败。

严格的数据外发限制与双路要求发生冲突时，应先满足已授权的数据边界；不能因为联网开关开启，就让原生搜索自动获得全部笔记。这里尚需明确不同隐私场景下哪些资料可发送、如何构造获准的查询。此例外不能成为普通场景退回单路择优的理由。

另外，用户明确接受免费工具与用户自行付费的优质服务并存，也接受申请搜索服务账户、填写一次 Key 并承担使用费。这排除了“只能选完全免费服务”的限制。

---

## 十一、循环控制与 LOOP 工程：重点是有效反馈，不是持续运行本身

用户提出此前曾讨论 LOOP 工程理论在项目中的应用。经过澄清，用户指的是：

> Iris 产品内部，让 agent 持续行动、验证、纠错，直到完成任务。

不是自动驱动 AI 开发 Iris 的工程流程。

Codex给出的判断是：这方面值得投入，但应落实在现有循环的反馈质量上。公开 Loop Engineering 讨论中的目标、观察、检查、纠错和停止思想可以借鉴；本轮没有把它视为必须引入某个新框架的理由。[实践讨论](https://addyosmani.com/blog/practical-loop-engineering/)

比较过的方向包括：

- 增加模型轮数和重试次数。
- 加固定反思模型或审核 agent。
- 新建独立规划器或研究循环。
- 在现有循环中明确目标、真实观察、验证标准、纠错依据和停止原因。

Codex倾向最后一种。

讨论形成的关键分工是：

- **审计**：发生了什么。
- **验证**：哪些要求已满足，还缺什么。
- **循环控制**：下一步应继续、修正、澄清还是结束。

不同任务需要不同验证：

- Markdown 内容保持适合程序化差异检查。
- 网页事实需要来源支持。
- 写入需要确认范围与文件版本校验。
- 文风与审美需要用户判断，模型自评只能辅助。

**当前建议：**将 LOOP 思想作为现有内核的整理原则，重点增强验证、反馈和有效纠错。

**没有形成的决定：**增加无限循环、固定反思调用、多 agent 审核或独立研究引擎。

### 验证是否必须先增加预算

DeepSeek 第一轮认为验证都需要额外模型轮次和网络调用，因此 LOOP 必须以增加网络额度为前提。第二轮已收回这一概括：重新搜索、补读网页需要相应预算，内容差异、文件版本与权限检查则可以由程序完成，不必增加模型或网络调用。

当前建议是让验证成本与具体任务相匹配。继续行动应针对明确缺口；重复同样失败、没有新反馈的调用，不因轮数更多而变得有效。停止应能说明任务完成、需要澄清、权限受限、服务失败或具体预算耗尽，而不是统一解释为模型能力降级。

### 预算问题从何而来，复核确认了什么

原纪要讨论过预算投影与提前收束，却没有充分解释额度的单位和作用范围。DeepSeek 的批注把预算提升为重要议题，这一补充保留；其中错误的页面算术与历史归因已撤回。

至少需要区分以下口径，它们数值可能偶然相同，但不能互相替代：

| 口径               | 含义与范围                                                 |
| ------------------ | ---------------------------------------------------------- |
| 工具提议           | 模型提出的调用，可能被拒绝或延后，不等于实际执行           |
| 主循环工具派发计数 | 已进入受控派发路径的调用；Host 保底调用也有计入逻辑        |
| 子任务预算         | 子循环按 child policy 运行，不能直接视为共用主循环六次额度 |
| Broker 内部请求    | 一次工具调用内部可能访问多个 URL、重试或切换服务商         |
| 页面数量           | 实际请求、成功读取、获得正文的页面数也应区分               |
| 服务计费           | 由供应商计费口径及实际请求、内容用量决定                   |

**已核实事实（`2d357038`）：**

| 事实                                                                 | 准确解释                                                                        | 依据                                                                                                                                      |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Standard／Delegated／DurableApply 的 `max_network_tool_calls` 都是 6 | 主循环 Network 类工具额度；不是全部服务商请求、页面或父子任务网络活动的统一总数 | [RunBudgetPolicy](./src-tauri/src/ai_runtime/run_contract.rs)，`for_envelope`                                                             |
| `web_search`、`web_fetch` 属于 Network 类                            | 主循环的搜索与抓取派发共用该类别额度                                            | [工具目录测试](./src-tauri/src/ai_runtime/tool_catalog/tests.rs)                                                                          |
| `web_fetch` 接收 `urls` 数组                                         | 一次调用可处理多页；主路径按待抓 URL 数计算单次上限，最多 8                     | [网页工具 schema](./src-tauri/src/ai_runtime/tool_catalog/web.rs)、[生产执行](./src-tauri/src/ai_runtime/run_tool_loop.rs)，`max_fetches` |
| 搜索初始索取 8 条，每次候选保留上限 4，Run 候选集合上限 8            | 候选发现与保留限制；不等于最终引用数或所有可直接读取 URL 的集合                 | 同上，`INITIAL_WEB_SEARCH_RESULTS`、`MAX_WEB_CANDIDATES_PER_DISCOVERY`、`remember_candidate_urls`                                         |
| Run 网页证据状态的额度初值 12，域名呈现上限 8                        | 证据容量与呈现约束，不能由此声称有这么多独立可靠来源                            | 同上，`MAX_WEB_EVIDENCE_PER_RUN`、`MAX_SOURCE_DOMAINS`                                                                                    |
| 单条网页工具结果上限 32,000 字符，网页摘录上限 2,000 字符            | 局部输出限制，不代表模型整体上下文剩余容量                                      | [循环](./src-tauri/src/ai_runtime/agent_tool_loop.rs)、[网页结果](./src-tauri/src/ai_runtime/run_tool_loop.rs)                            |
| 子循环使用 `from_child_policy`                                       | 网络上限取 child policy 的工具额度，并维护子循环用量                            | [子循环预算](./src-tauri/src/ai_runtime/agent_tool_loop.rs)                                                                               |

`dispatch_non_web_tool` 上下文中的 `max_web_fetches: 5` 不属于网页主链路的全局抓取上限。第一轮附录将它列为“整轮抓五篇”，并推导只能引用少量来源，已撤回。

**当前判断：**固定额度是否限制某类任务，需结合实际派发、成功观察、失败恢复与剩余预算判断。六次偏紧是待验证风险，不是已经确认的统一根因；也没有证据表明候选截取等其他限制因此不重要。尚未接入的原生搜索如何计入额度同样待定。

### 预算调整与存档兼容

DeepSeek 查出并经 Codex 复核的一项有效补充是：当前读取逻辑会比较存档策略与规范策略，同版本但数值不匹配可能返回 `InvalidBudgetPolicy`。当前没有通用的 schema 3 旧数值兼容分支，不能假设修改默认常量后旧 Run 自动可读。依据：[存档预算读取](./src-tauri/src/ai_runtime/agent_run_repository.rs)，`materialize_budget_policy`。

**当前建议：**任何预算调整都要处理旧策略的识别、读取和恢复语义，不能无说明地给已有 Run 扩权或破坏读取。“升策略版本并增加兼容分支”是一种方向，不是本轮确定的唯一实现，也不等同于 SQLite 表结构迁移。

本轮没有设定新额度，没有放开上限，也没有批准预算修改顺序。

---

## 十二、上下文、压缩和记忆：保存聊天记录不等于保持任务连续性

用户主动提出，长对话可能超过模型上下文范围，因此必须考虑压缩和记忆。

讨论将“记忆”拆为四类：

| 类型               | 作用                           | Iris 当次审查状态        |
| ------------------ | ------------------------------ | ------------------------ |
| 当前工作上下文     | 当前请求、最新约束、工具观察   | 已有                     |
| 会话历史与摘要     | 保留此前对话，控制上下文规模   | 已有，但覆盖边界存在问题 |
| 长期偏好或事实记忆 | 跨会话保存适当信息             | 已有独立存储与读写工具   |
| 持续任务状态       | 保留已核实、已撤回、未完成事项 | 有部分基础，尚不完整     |

审查发现，近期历史受 token 和消息对数限制，模型压缩却主要按消息条数确定覆盖终点。少量长消息可能先被裁剪，但尚未进入摘要覆盖范围。

系统已有覆盖不完整的检测，因此不能说它完全静默丢失；但发现缺口与补齐缺口是不同能力。

另一个问题是，普通历史重建主要传递用户与助手文本，没有完整保留研究工作状态。后续追问能否准确承接，仍较依赖摘要。

对应实现见 [Run 上下文](./src-tauri/src/ai_runtime/run_context.rs) 的近期历史裁剪，以及 [会话摘要](./src-tauri/src/ai_runtime/conversation_memory.rs) 的 `pending_model_compaction`。把聊天记录存进数据库，与模型本轮实际收到什么，是不同事实。

**批注带来的补充：**DeepSeek 强调持续任务状态的重要性，这一方向保留。不过，不能把它写成前三类只是“存得下”、只有第四类负责“接得住”；正确承接依赖原消息、当前指令、摘要覆盖与任务状态共同一致。

应保留的语义至少包括当前目标、用户最新纠正、已撤回信息、已经执行的动作、未解决问题与证据时效。这是对连续性的要求，不预设必须新建一个独立状态模块。

“收紧联网次数会加速裁剪”的推论已撤回。联网次数、输入 token、工具结果长度、模型轮数和摘要覆盖是不同约束；更多调用也可能带来更大上下文压力。因此，预算与长对话之间的影响需要按实际数据流判断，不能预设因果方向。

**明确需求：**长对话应保留最新纠正、重要约束和任务进展。

**当前建议：**上下文裁剪、压缩范围、来源归属和任务恢复应一致；摘要不能把旧线索变成新证据，也不能覆盖用户后来的纠正。

**待讨论或验证：**长期记忆的写入时机、有效期、冲突处理、用户管理方式，以及执行中补充要求的具体交互。当前存在记忆存储，不表示这些产品行为都已确定。

---

## 十三、文档修改与结果发布：生成、确认、执行、验证应当分开

围绕三个编辑场景，讨论双方强调了不同任务的完成标准：

- 润色：候选文本符合对象、长度和明确偏好。
- 生成：候选内容适合目标位置和文风。
- 格式整理：结构发生变化，实际内容保持，序号按用户允许处理。
- 应用修改：真实执行与用户确认的目标、范围和版本一致。

因此，模型说“已经整理好了”或“没有改变内容”，不能代替实际检查。

Iris 已有冻结变更计划、权限与确认、文件 hash 校验等基础。这些属于与文档产品紧密结合的运行能力，是整体框架替换时最需要保留的部分之一。

同时，普通回答应自然完成。不能为了让所有任务都满足同一套终局格式，给简单对话增加不必要的失败点。

**目前结论：**任务类型决定验证方式；实际文件写入继续由 Host 控制。生成候选与执行写入之间的边界不应被模型自主性模糊。

---

## 十四、内部审计与故障诊断：现有记录能看到动作，未必能还原因果

用户明确提出：是否内部审计不到位，导致每次错误都容易只剩猜测？

审查后的回答是：**故障可诊断性确实不足，但问题不仅是少一个审计模块。**

现有工具审计保存运行、步骤、工具、摘要、成功状态和耗时；循环诊断保存部分计数、首次失败及最近 12 条事件；Run 事件主要服务安全的界面回放。

这些记录有价值，却存在几个缺口：

- 较长运行的中间恢复过程可能被挤掉。
- 同一工具多次调用、重试和父子任务之间的关联不够完整。
- 上下文进入网关前后是否保持一致，缺少关键边界证据。
- 记录“追加了修复提示”，不等于证明最终请求真的发送了提示。
- `unknown` 工具记录能指出拒绝位置，却不能解释名称从何处产生。

依据：[工具审计](./src-tauri/src/ai_runtime/tool_audit.rs)、[循环诊断](./src-tauri/src/ai_runtime/run_tool_loop.rs)。

讨论提出，一次失败至少应区分：

1. 直接失败位置。
2. 恢复过程及其结果。
3. 已确定或尚未确定的根因。

“恢复耗尽”是恢复结果，不是根因；“工具未知”是直接失败，也不能直接归咎于模型。

**当前建议：**沿已有执行链补齐关联和交接证据，分别记录事实、策略决定和最终结果。诊断应能明确说出已知、已排除及缺失证据。

这不需要记录用户笔记、凭证或完整私有推理，也不建议增加一个让 AI 猜根因的审计 agent。

**后续补充：出站请求是重要的交接边界。** DeepSeek 提议记录出站 `messages`／`tools` 的结构摘要或哈希。复核后进一步明确：只有哈希不足以解释字段缺失；更有用的是安全的结构证据，如消息角色与数量、必要字段是否存在、修复消息是否被纳入、所用协议，以及前后边界的关联标识。

这些记录需与 Run、模型轮次、尝试、工具调用及父子任务建立联系。诊断应区分“业务层生成了反馈”“网关序列化了反馈”“请求已发送”“供应商返回了什么”；前一事实不能冒充后一事实。记录也需遵守最小化与脱敏约束，不能把模型自造字符串或私有正文未经处理地写入日志。

判断“模型是否真的理解修复”仍属于行为评价，出站记录只能证明必要信息的传递，不能证明语义理解。

**认识边界：**协议丢字段可以机械定位；推荐偏题等语义问题可能由多个因素共同导致，仍需受控复现与人工评价。

---

## 十五、Harness 与评测：为什么测试通过，用户仍然觉得不好用

审查发现，部分连续性压力测试使用的替身模型只返回确认文本，最终检查的是关键约束是否出现在历史或摘要中，没有要求模型真正依据这些约束完成任务。

确定性能力测试也大量依赖预设模型和工具轨迹。它们适合验证管线合同，但不能独立证明真实模型能合理选工具、理解修复反馈和完成自然追问。

真实 pilot 已有连续会话和人工评分，这是值得保留的基础；但题目普遍明确要求“请联网核实”，与用户自然表达的原始链路并不等价。[连续性测试与 pilot](./src-tauri/src/ai_runtime/agent_capacity_eval/test_support.rs)

本轮还出现了一个典型现象：模型网关 68 项测试全部通过，但其中存在把有问题的请求结构作为正确期望的测试。说明测试和实现可能共享同一个错误假设。

因此，讨论双方区分了三类证据：

| 证据           | 能证明什么                                 |
| -------------- | ------------------------------------------ |
| 单元与协议测试 | 某个边界是否按指定合同工作                 |
| 组合轨迹测试   | 上下文、模型、工具、观察和终态是否正确衔接 |
| 真实任务评价   | 回答是否相关、准确、完整，并遵守最新纠正   |

**当前建议：**三者共同构成质量依据；测试数量、编译通过和运行结束都不能替代用户目标完成。

本轮实际完成的验证包括模型网关 68 项测试、harness 脚本 16 项测试，以及若干隔离函数验证。没有运行新的付费实网评测，也没有据此宣布完整产品验收通过。

上述数字是 2026-09-14 首轮审查的历史验证记录，不是本次文档整理重新执行的全套测试结果。

### 完成标准与自动评价的边界

DeepSeek 第一轮指出，只有“测试通过不等于任务完成”的提醒，还不足以指导判断，这一补充有价值。但其“机械检查过了即整个功能完成”“语义质量机器测了也没用”两种绝对说法，均在第二轮撤回。

| 目标性质   | 可以依赖的证据                             | 证据边界                                           |
| ---------- | ------------------------------------------ | -------------------------------------------------- |
| 机械不变量 | 内容差异、目标绑定、版本、授权等程序检查   | 证明被检查条件在覆盖范围内成立，不外推整个产品完成 |
| 组合行为   | 真实执行边界、输入输出与恢复轨迹的组合测试 | 可以证明相应组合合同，不能凭连通性推断回答质量     |
| 语义质量   | 明确评分标准、自动评价、人工校准和抽检     | 自动评价有用但会误判；文风满意度等仍需用户判断     |

本轮已有的任务判据包括：格式整理不改实际内容（序号按允许处理）、生成内容符合目标位置、追问承接最新约束、时效回答符合日期与地域且有来源支持。它们还没有组成最终发布门槛。不能靠不显示错误提示或给出流畅文字代替这些要求。

### 对照实验的讨论及其局限

DeepSeek 提议更换模型、放宽预算、同时改变两者的三臂比较。第二轮接受需要同期原配置基线，形成四组比较思路：

| 比较组     | 模型     | 预算               | 可观察的问题                   |
| ---------- | -------- | ------------------ | ------------------------------ |
| 原配置基线 | 原模型   | 原预算             | 同期任务表现与故障轨迹         |
| 模型变化   | 替代模型 | 原预算             | 更换实际模型配置后表现如何变化 |
| 预算变化   | 原模型   | 明确指定的预算变化 | 目标额度是否限制了有效继续行动 |
| 两者变化   | 替代模型 | 同一预算变化       | 两项变化是否存在共同影响       |

这只是尚未执行的比较建议，不是施工安排。换模型可能同时改变协议适配、工具调用习惯及输出结构，不能直接判为“模型的锅”。只调整网络次数也不测试两轮拒绝阈值、上下文或时间预算。必须说明究竟改变哪一项，比较对象才清楚。

预算存档校验又意味着“无需改代码，临时放开即可”尚无已核实的入口支持。实验需要隔离配置与数据，并保持其他条件尽量一致；本轮未给出已可运行的实验实现。

将用户的“寒暄→问时间→问近期电影→追问”经历固定为案例是合理建议，但当前文字是经历概述，不是逐字完整 transcript。后续重建话术应标为重建案例，并注明日期、地区和配置，不能冒称原始请求重放。除该案例外，还需避免只按实现偏好出题。

“连续十次通过”只可作为初步稳定性观察，尚未成为用户确认的验收门槛。问题相关性、时效、准确性、来源与追问承接需要分别评价；单一话术通过不能证明广泛可用。

---

## 十六、子 agent：存在意义与当前实现收益必须分开判断

用户指出项目已有子 agent，追问它是否有用、是否值得存在。

Codex给出的判断是：

- 对独立的大量阅读、多方向研究和范围明确的复核，它有潜在实际价值。
- 对基本聊天、段落润色、单段生成和 Markdown 格式整理，通常不是必需能力。
- 多开一个模型调用，不会自动带来独立性或更高准确率。

当前实现具有最多三个子任务、只读工具限制、禁止递归、独立续轮和报告回传等基础。但进一步检查发现：

- 子工具清单包含 `web_search`，没有 `web_fetch`，限制了标准网页正文核验。
- 工具定义提供 `context_hint`，执行路径却没有消费它。
- `max_rounds` 没有作为实际轮次控制输入，执行采用冻结预算。
- 报告置信度按是否有 evidence ID 固定填 50 或 75，没有事实准确率依据。
- 子任务使用同一 Provider，主要接收主 agent 写出的任务描述，可能继承错误前提。

依据：[子任务协调与报告](./src-tauri/src/ai_runtime/subagent_coordinator.rs)、[生产执行路径](./src-tauri/src/ai_runtime/run_tool_loop.rs)。

**当前建议：**保留为受限可选能力，暂不扩展到普通任务；其价值应通过具体任务的单 agent／子 agent 对照验证。

**没有形成的决定：**全面删除子 agent，或将多 agent 设为默认架构。是否长期保留现有全部实现，也未锁定。

**DeepSeek 复核与观点变化：**第一轮确认上述工具与参数事实后，曾推导“子 agent 结构上无法读取正文，尚不具备任何对照条件”。第二轮已纠正：缺失的是标准网页正文抓取能力；`read_note` 等本地工具仍可读取材料，不能因此否定本地阅读、独立分析或范围明确复核的可能价值。

当前实现可以参与对照，以衡量它现在是否有收益；若要据此判断多 agent 架构的潜力，则必须说明能力缺口和比较条件，不能把受限实现的失败推成整个思路无效。比较还需交代模型、任务、总成本、耗时和上下文条件，而非只比较最终回答长度。

`context_hint`、`max_rounds` 在 schema 中存在却未被消费，是独立的接口合同缺陷。收清合同可能意味着实现这些参数，或明确移除／限制不支持的参数，并非必须照单实现全部接口。固定置信度 50／75 也不是校准后的准确概率。上述事实不需要等待“子 agent 是否值得保留”的实验才能承认，但处置方式尚未选定。

---

## 十七、框架替换：观点如何变化，当前倾向是什么

最初候选包括 Mirascope、Vercel AI SDK、PydanticAI、smolagents，并允许继续扩展范围。它们代表了不同层次的能力：

| 候选方向                       | 与讨论的关系                  |
| ------------------------------ | ----------------------------- |
| Mirascope                      | 模型调用、工具和组合抽象      |
| Vercel AI SDK／`ToolLoopAgent` | 模型适配及工具循环            |
| PydanticAI                     | 带类型约束的 agent 与工具编排 |
| smolagents                     | 轻量 agent 及不同动作执行方式 |
| Rust 生态或其他轻量实现        | 在现有后端约束下寻找局部替代  |

相关产品定位见 [Mirascope](https://mirascope.com/docs/learn/llm)、[AI SDK](https://ai-sdk.dev/docs/reference/ai-sdk-core/tool-loop-agent)、[PydanticAI](https://pydantic.dev/docs/ai/overview/)、[smolagents](https://huggingface.co/docs/smolagents/main/index)。

讨论中，Codex早期曾偏向使用 AI SDK 等成熟实现迁移。深入审查后，Codex明确收回了“优先整体迁移”的过早判断。

原因是：

- Iris 已有值得保留的持久化、权限、确认、工具执行及证据基础。
- 多个关键问题可以定位到协议、上下文和控制逻辑，并不要求整体换框架。
- 原有查询预判、裁剪和退出策略如果原样迁移，问题可能继续存在。
- 项目技术栈与数据边界增加了整体迁移成本。

目前比较后的倾向是：

| 部分                                       | 当前建议                           |
| ------------------------------------------ | ---------------------------------- |
| 运行持久化、权限、确认、文档执行、证据账本 | 保留                               |
| 模型协议、流式解析、必要续轮状态           | 优先评估成熟实现替代部分自研       |
| 查询理解、修复反馈、预算和停止策略         | 整理现有职责，减少相互矛盾的控制   |
| 完整 agent 框架                            | 暂不启动整体替换，保留后续比较空间 |

**这是一项当前建议，不是已批准的选型决定。** 本轮没有选定具体依赖、迁移方式或最终框架，也没有证明现有循环永远不需要替换。

DeepSeek 的两轮批注均认可暂不整体迁移。预算争议的订正没有带来必须全面替换的新证据，也没有证明继续全部自研最优。

仍然保留的选择包括修正现有实现、局部引入成熟协议或循环实现，以及在边界和效果证据支持时重新评估整体迁移。后续比较需要能说明替代的是哪一层、减少了哪些维护责任、保留了哪些 Iris 合同，以及在主模型和真实任务上表现如何。候选框架的功能表和知名度本身不足以选型。

本轮也未完成所有候选的接入原型、许可证逐项审查、打包成本或生产兼容验证。表中的候选范围不能被解读为已批准的依赖清单。

---

## 十八、有效结论与未决事项

下表是截至整理日期的有效状态汇总。历史争议见附录；已撤回观点不构成额外前置条件或隐含决定。

### 用户已明确的要求与边界

| 议题                                                         | 有效状态                                                      | 对应讨论 |
| ------------------------------------------------------------ | ------------------------------------------------------------- | -------- |
| 基础对话、话题切换、追问和纠正可靠                           | 明确需求，是高级研究能力之前的基本可用性要求                  | 一、二   |
| 字词／句段润色、扩缩写与文风匹配生成                         | 明确需求；新内容通常由用户插入                                | 二、十三 |
| Markdown 格式整理不修改实际内容，序号除外                    | 明确内容保持要求                                              | 二、十三 |
| MiniMax、DeepSeek 为重要主模型，兼顾 Gemini Flash 和国产模型 | 明确偏好与兼容目标，未限定只能支持这些模型                    | 六       |
| 支持且获准时原生与 MCP 两路都实际搜索                        | 多次纠正后明确；不能退回两路可选或一主一备                    | 八、十   |
| 两边发现的 URL 均可由 MCP 抓取                               | 按需要选择读取，不是全部抓取；无需先由 MCP 重搜               | 八、九   |
| 不支持原生搜索时正常使用 MCP                                 | 不支持是正常配置，不应显示能力降级；真实证据不足仍需说明      | 十       |
| 严格场景保留联网限制                                         | 授权应覆盖原生、MCP、抓取、重试及子任务；外发资料边界另需明确 | 三、十   |
| 开箱即用工具与用户自行付费服务并存                           | 用户接受申请服务账号、填 Key 和费用，不要求只能免费           | 十       |
| 长对话需要压缩、记忆及最新约束承接                           | 明确需求；具体管理与交互未完全确定                            | 十二     |
| LOOP 指产品内部持续行动、验证和纠错                          | 用户已明确其讨论范围；具体机制仍属建议                        | 十一     |

### 已核实事实及其解释边界

| 议题                          | 有效状态                                                        | 不能据此推出                                   |
| ----------------------------- | --------------------------------------------------------------- | ---------------------------------------------- |
| 历史未知工具提议后修复收束    | 已观察到；当次三轮模型、两次工具调用，存在独立两轮拒绝规则      | 六次网络额度已用完；模型或 MCP 是唯一根因      |
| 主循环 Network 额度为 6       | 三个生产预设的代码事实；页面、内部请求、子循环另计              | 最多抓四篇／五篇；整个父子任务总共只能联网六次 |
| `web_fetch` 支持 URL 数组     | 可批量读取，主路径单次抓取计算上限为 8                          | 一定抓满、全部成功、所有页面完整阅读           |
| 预算读取比较存档与规范策略    | 直接改默认数值有旧 Run 读取兼容风险                             | 必须采用唯一迁移实现；必须修改 SQLite 表结构   |
| 子 agent 参数声明与执行不一致 | `context_hint`、`max_rounds` 存在未消费问题；固定置信度未经校准 | 子 agent 整体没有价值或不能做任何对照          |
| 子工具没有标准 `web_fetch`    | 网页正文核验受限，本地读取工具仍存在                            | 无法读取任何正文                               |
| Host 联网保底触发顺序已变化   | `3a8c1903` 延后至终局校验边界                                   | 电影推荐、地域默认或多轮质量已经实测通过       |
| 存在协议、上下文及诊断问题    | 分别按本轮代码和历史审查证据理解                                | 所有故障同因，或某个库必然全部解决             |

### 当前建议与尚未确定的选择

| 议题                                           | 有效状态                                                 | 对应讨论   |
| ---------------------------------------------- | -------------------------------------------------------- | ---------- |
| 保留运行持久化、权限、确认、文档执行与证据基础 | Codex 当前建议；DeepSeek 批注认可，非用户批准的最终选型  | 十七       |
| 优先评估协议与必要续轮信息的成熟实现           | 当前建议；具体依赖、语言接入和替换范围未选定             | 六、十七   |
| 用 LOOP 思想改善真实反馈、验证与停止原因       | 当前建议；不是无限循环或固定审核 agent                   | 十一       |
| 补充出站结构诊断及调用关联                     | 当前建议；哈希不能替代结构信息，也不能证明模型理解       | 十四       |
| 依据来源质量处理双路冲突                       | 当前建议；具体自动判断机制未确定                         | 八         |
| 子 agent 作为受限可选能力                      | 当前建议；收益、成本与能力缺口需分开评价                 | 十六       |
| 网络额度是否为主要瓶颈、新额度取多少           | 待验证；不作为双路与 LOOP 的无条件前置                   | 十一       |
| 历史未知工具提议的唯一根因                     | 未确定，缺少足以区分生成／解析／映射／提示问题的历史证据 | 五、十四   |
| 原生搜索端点、混合续轮、费用与权限适配         | 待验证；不能仅凭厂商名判定                               | 六、十     |
| 双路对纯编辑任务的触发范围及时间并行要求       | 尚未单独确认；不改变两路实际执行的已明确要求             | 八         |
| 长期记忆写入、有效期、冲突与用户管理           | 尚未完整确定                                             | 十二       |
| 运行中追加／修改指令的交互及状态处理           | 尚未完整确定；不能把讨论意愿写成已实现能力               | 二、十二   |
| 模型与预算对照、子 agent 对照                  | 建议尚未执行；实验条件与评价规则需要明确                 | 十五、十六 |
| 发布判据与数值门槛                             | 本文不新设门禁；“连续十次”仅为讨论过的初步观察建议       | 十五       |

---

## 附录：观点变化、采纳与撤回记录

本表保留原纪要、DeepSeek 第一轮批注、第二轮订正及 Codex 复核中的主要变化。采用“原观点—为何变化—有效状态”的形式保存讨论历史，避免旧正文、局部撤回标记与结尾状态表彼此冲突。

旧观点在本表中出现不表示仍然有效。用户要求、代码事实和助手建议分别以正文及第十八节为准。

| 来源或议题                    | 当时的观点                                                    | 复核或讨论依据                                                         | 本次整理后的状态                                                         |
| ----------------------------- | ------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| Codex 早期框架比较            | 偏向 AI SDK 等整体迁移                                        | 深入审查发现 Iris 已有持久化、权限与文档合同；故障可定位到多个局部边界 | **早期倾向已收回**；保留底座、局部评估替代是当前建议，见十七             |
| Codex 早期联网建议            | 两路可用后由模型择优，或 MCP 补充抓取                         | 用户多次明确两路都实际使用，MCP 也独立搜索                             | **已被用户纠正**；双路实际搜索、共享按需抓取，见八                       |
| DeepSeek 批注 1               | 网络六次额度可解释那次恢复耗尽，并应取消“不倒推历史”的限制    | 历史调用数与独立两轮拒绝规则不支持该归因                               | **该因果推论已撤回**；额度风险与恢复规则分别讨论，见五、十一             |
| DeepSeek 批注 2               | 搜索事件和修复消息的传递是双路与 LOOP 的前提                  | 输入与观察无法传递，就不能判断上层策略效果                             | **依赖判断保留**；不扩大为所有设计必须暂停，见六                         |
| DeepSeek 批注 3               | 原生一次 + MCP 一次 + N 页 = 2+N 次，最多四页，双路无法实现   | 批量抓取、内部请求与主循环计数不同；需求也不是全部抓取                 | **算术与前置结论已撤回**；不能据此要求先放宽预算，见八、十一             |
| DeepSeek 批注 3 的召回解释    | MCP 覆盖窄已核实，第二路必然改善召回                          | 用户体验、服务召回、查询质量及 Host 截取未被分离验证                   | **不作为已核实效果结论**；双路需求保留，实际增益待验证，见八             |
| DeepSeek 批注 3 的冲突补充    | 应补充两路事实冲突处理                                        | 多渠道可能同源或口径不同，数量不能代替证据质量                         | **建议保留并收清**；按出处、时效、地域、口径和正文判断，见八             |
| DeepSeek 批注 3 的成本判断    | 两路导致费用和延迟翻倍                                        | 定价、批量、并行、重试及等待结构不同                                   | **固定倍数结论撤回**；成本与时间需实际衡量，见八                         |
| DeepSeek 批注 4               | 验证均需额外联网，LOOP 必须先增加额度                         | 部分验证是确定性本地检查，资源需求不同                                 | **普遍前置已撤回**；按任务核算验证成本，见十一                           |
| DeepSeek 批注 5               | 持续任务状态重要；收紧联网次数会加速裁剪                      | 前者是有效需求提醒，后者没有直接数据流依据                             | **保留连续性关注，撤回单向因果**，见十二                                 |
| DeepSeek 批注 6               | 保存出站结构或哈希即可诊断字段丢失                            | 哈希不能单独说明缺哪个字段，仍需边界关联与安全结构信息                 | **建议保留并细化**，见十四                                               |
| DeepSeek 批注 7               | 机械测试过了即完成，语义质量自动评价没用                      | 测试证明有范围；自动评价有价值但需要校准                               | **绝对表述已撤回**；按目标与证据范围判断，见十五                         |
| DeepSeek 批注 7 的案例建议    | 将真实经历固定为未污染的考卷                                  | 有用户经历，但本文没有完整逐字请求；单一案例还不足以验收               | **建议保留并限定**；区分原始记录与重建案例，见十五                       |
| DeepSeek 批注 8               | 无网页抓取说明子 agent 无法读正文，不能参加对照               | 仍有本地读取能力；当前实现收益与架构潜力是不同问题                     | **过度结论已撤回**；保留网页能力缺口与参数合同事实，见十六               |
| DeepSeek 批注 9               | 三臂可直接判定模型／预算责任，不需代码支持即可调额度          | 缺同期基线，换模型可能换协议路径；临时预算入口未核实                   | **实验建议保留并限定**；不声称能直接归因或已有实验入口，见十五           |
| DeepSeek 批注 9 的门槛        | 十次通过才可进入下一阶段；协议处理应等实验                    | 单例通过不等于广泛稳定；已知合同问题不需等待对照才承认                 | **不采纳为门禁或工程前置**，见六、十五                                   |
| DeepSeek 批注 10              | 预算已构成缺陷，子 agent 尚无对照条件，升级 schema 是唯一前提 | 上述判断的论证已撤回或范围已缩小                                       | **旧状态表失效**；统一采用第十八节                                       |
| DeepSeek 附录的抓取上限       | 非网页派发上下文中的 5 是整轮网页上限                         | 并非网页主链路计数，不能仅靠常量命中推断                               | **已撤回**，见十一                                                       |
| DeepSeek 附录的信源及字符推论 | 五页限制解释单一信源；约一万字符说明上下文充足                | 页面计数前提错误，缺历史轨迹，且未计完整输入                           | **已撤回**；候选、正文、引用和上下文分别核对，见十一、十二               |
| DeepSeek 附录的存档问题       | 同版本不同数值有读取风险；必须升版本并迁移                    | 读取风险成立，具体兼容方法不唯一，也不等于数据库表迁移                 | **事实与兼容责任保留，唯一方案表述撤回**，见十一                         |
| DeepSeek 初次提交归属         | 本地 FTS 两个提交修复网页地域问题                             | 提交内容对应本地检索，相关执行顺序变化是 `3a8c1903`                    | **错误归属已撤回**；不声称地域质量已实测修复，见五                       |
| 文档修订方式                  | 在开头订正，保留后面旧批注与旧状态表                          | 局部阅读仍可能把旧结论当成当前要求                                     | **本次已统一表达**：正文呈现有效判断，本表保留历史，不叠加相互冲突的状态 |
| 文档链接                      | 聊天中的本机路径直接作为仓库链接                              | 跨机器和普通 Markdown 浏览不可移植，行号也随代码变化                   | **本次改为相对路径与关键符号定位**                                       |

本文的用途是为下一阶段提供可追溯的判断依据：用户需求保持明确，已知事实不扩大为未经证明的根因，建议允许被验证推翻，未决事项不被写成既定方案。修订讨论记录不等于修复 Iris，也不等于完成真实模型或产品验收。
