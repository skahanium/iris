name: sqlite-vec Scale Ladder

on:
  schedule:
    - cron: "0 19 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sqlite-vec-scale-ladder-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sqlite-vec-scale-ladder:
    name: sqlite-vec 1k/10k/25k/50k KNN ladder
    runs-on: ubuntu-24.04
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Run sqlite-vec KNN scale ladder
        working-directory: src-tauri
        run: cargo test --features sqlite-vec embedding::engine::tests::sqlite_vec_knn_scale_ladder -- --nocapture
